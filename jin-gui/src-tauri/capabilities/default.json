{
  "identifier": "default",
  "description": "Default capabilities for Jin GUI — allows the main window to invoke commands",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
